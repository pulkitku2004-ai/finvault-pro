# FinVault AI — Python Dependencies
# Requires Python 3.12 exactly (3.13+ breaks unstructured dependencies)
# Setup: python3.12 -m venv .venv && source .venv/bin/activate
# Install: pip install -r requirements.txt
# Spacy model: python -m spacy download en_core_web_sm
#
# System dependencies (macOS):
#   brew install tesseract poppler
# System dependencies (Ubuntu/Debian):
#   apt-get install tesseract-ocr poppler-utils

# ── LLM & LangChain ──────────────────────────────────────────────────────────
langchain==1.2.15
langchain-core==1.2.27
langchain-openai==0.3.27
langchain-ollama==1.1.0
langchain-chroma==1.1.0
langchain-text-splitters==1.1.1
langchain-community==0.4.1

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb==1.5.7

# ── Retrieval ────────────────────────────────────────────────────────────────
rank-bm25==0.2.2
sentence-transformers==5.3.0

# ── Knowledge Graph ──────────────────────────────────────────────────────────
neo4j==6.1.0
spacy==3.8.14

# ── PDF Parsing ──────────────────────────────────────────────────────────────
# Note: do NOT install the old 'pdfminer' package — it conflicts with pdfminer.six
unstructured==0.18.32
unstructured-inference==1.6.4
unstructured.pytesseract==0.3.15
pdfminer.six==20260107
pi-heif==1.3.0
pdf2image==1.17.0
pytesseract==0.3.13

# ── ML / Torch (cross-encoder reranker + unstructured layout models) ─────────
torch==2.11.0
torchvision==0.26.0
transformers==5.5.0

# ── API & UI ─────────────────────────────────────────────────────────────────
fastapi==0.135.3
uvicorn==0.44.0
streamlit==1.56.0
pydantic==2.12.5
requests==2.33.1

# ── Evaluation ───────────────────────────────────────────────────────────────
ragas==0.4.3
datasets==4.8.4
pandas==3.0.2

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
httpx==0.28.1
